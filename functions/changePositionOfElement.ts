import {Editor} from '../entities/Editor'
import {Position} from '../entities/Position'

export {
    changePositionOfElement
}

function changePositionOfElement(redactor: Editor, slideId: number, elementId: number, newPosition: Position): Editor {
    return redactor
}