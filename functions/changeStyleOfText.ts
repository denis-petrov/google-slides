import {Editor} from '../entities/Editor'
import {TextStyle} from '../entities/TextStyle'

export {
    changeStyleOfText
}

function changeStyleOfText(redactor: Editor, slideId: number, elements: Array<number>, textStyle: TextStyle): Editor {
    return redactor
}
